# Only Confirmed Trend / 只做确定趋势

> 四层过滤：一个方向，一次入场，趋势不确认就不动。
> *Evolution of [Less-is-More V1.0](https://github.com/okx-trading/skill-less-is-more)*

---

## 🎯 策略一句话

在 EMA 趋势方向确认的基础上，用 **ADX ≥ 25** 确认趋势正在持续，再用市场结构和 ATR 动量双重验证入场时机——四个条件同时满足才出手，其余时间什么都不做。

---

## 📊 核心指标

| 指标 | 数值（ETH 90天回测）|
|------|---------------------|
| 初始资金 | $10,000 |
| 最终净值 | $9,360（V1.0基准）|
| 总收益率 | -6.40%（V1.0）|
| 最大回撤 | -11.22% |
| 总交易次数 | 20（V1.0）|
| 胜率 | 20% |
| 核心问题 | 震荡市反复止损 |

> V2.0 通过增加 ADX ≥ 25 过滤层，预期将大幅减少震荡市交易次数，提升胜率和盈亏比。

---

## 🔬 四层过滤系统

```
┌─────────────────────────────────────────────────────────────┐
│  第一层 · EMA 趋势方向                                        │
│  EMA21 > EMA55 > EMA200（多头）                              │
│  EMA21 < EMA55 < EMA200（空头）                              │
│  → 判断：趋势方向是否存在？                                    │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  第二层 · ADX 趋势持续性确认 ⭐ NEW                          │
│  ADX >= 25 → 趋势确认，允许入场                               │
│  ADX <  25 → 跳过（震荡市/趋势形成中）                        │
│  ADX >= 40 → 注意仓位，降低上限                              │
│  → 判断：趋势是否真的在持续？                                 │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  第三层 · 市场结构（入场时机）                                 │
│  看多：HL回调（SL1>SL2）或 BOS突破（>SH1）                   │
│  看空：LH反弹（SH1<SH2）或 BOS跌破（<SL1）                   │
│  → 判断：入场时机是否合适？                                   │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  第四层 · ATR 动量确认                                       │
│  0.7x <= ATR倍率 <= 1.8x → 正常，允许入场                    │
│  ATR倍率 < 0.7x → 跳过（市场睡着了）                         │
│  ATR倍率 > 1.8x → 跳过（波动过大）                           │
│  资金费率 |fee| > 0.1% → 跳过（方向已过度拥挤）              │
│  → 判断：市场是否有足够动量？                                 │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
                    ✅ 四层全过 → 入场
```

---

## 📦 安装

### 方法一：OKX Skill Marketplace（推荐）

```bash
okx skill add only-confirmed-trend
```

### 方法二：手动安装

```bash
# 克隆仓库
git clone https://github.com/<your-username>/only-confirmed-trend.git
# 放入 agent skills 目录
cp -r only-confirmed-trend ~/.agents/skills/
```

---

## ⚙️ 使用方法

在 OKX Agent Trade Kit 中，直接触发：

```
只做确定趋势
确认趋势入场
四层过滤交易
ADX趋势策略
```

AI 会按顺序执行四层检查，并生成如下检查表：

```
【四层过滤检查表】

第一层 · EMA 趋势：       [✅ 看多 / ❌ 混乱]
第二层 · ADX 趋势持续性：  [✅ ADX=28.5 / ❌ ADX<25]
第三层 · 市场结构：        [✅ HL回调 / ❌ 无信号]
第四层 · ATR 动量：       [✅ ATR倍率=1.2 / ❌ 波动异常]

最终决策：[✅ 入场做多]  理由：EMA多头+ADX确认+HL回调+ATR正常
```

---

## 📈 止盈止损规则

```
入场后立即挂单：

止损（SL）= 入场价 ± 2 × ATR14
TP1       = 入场价 ± 3 × ATR14  → 平 60% 仓位
TP2       = 入场价 ± 5 × ATR14  → 平 40% 仓位

TP1 触发后 → 止损自动移至入场价（保本）
```

---

## 🛡️ 风控总则

```
[P1] 单笔最大风险：账户净值 × 1.5%
[P2] 当日回撤超 4.5% → 停止新开仓
[P3] 最多同时持有 1 个仓位
[P4] 开仓后 60 秒内完成止损挂单
[P5] 严禁在亏损时加仓，严禁移动止损
[P6] 单日最多 3 次入场机会
[P7] ADX >= 40 时，最大仓位上限降至 5%
```

---

## 🔄 持仓监控规则

持仓期间每次检查循环执行：

1. **ADX 持续下降？** ADX 从 ≥ 30 回落至 < 20 → 立即平仓 ⭐
2. **EMA 反转？** EMA21 穿越 EMA55 → 立即平仓
3. **止盈/止损单丢失？** → 立即补挂

> ADX 退出保护是 V2.0 相比 V1.0 的重要改进——它比 EMA 反转更早发出退出信号。

---

## 📋 V1.0 vs V2.0 对比

| | V1.0 少即是多 | V2.0 只做确定趋势 |
|---|---|---|
| 过滤层数 | 3 层 | **4 层** |
| 新增 | — | **ADX ≥ 25 趋势确认** |
| 震荡市交易 | 较多（易被打止损）| **大幅减少** |
| 趋势市交易 | 正常 | 正常 |
| ADX 退出保护 | ❌ 无 | **✅ 有** |
| 信号频率 | 较高 | 较低 |
| 预期胜率 | 较低 | **较高** |
| 预期盈亏比 | 较低 | **较高** |
| 适用人群 | 接受震荡损耗 | 追求高确定性 |

---

## 📂 文件结构

```
only-confirmed-trend/
├── SKILL.md          # 策略核心指令文件（AI 读取）
├── _meta.json        # 元数据
├── README.md         # 本文件
└── LICENSE           # MIT License
```

---

## 📝 回测明细（V1.0 基准参考）

回测标的：ETH-USDT-SWAP
回测周期：2026-01-15 ~ 2026-04-15（89天）
初始资金：$10,000
K线频率：1h

**盈利交易（4笔）：**
- SHORT @1970.80 → TP2 @1855.20  **+$117.31**
- LONG  @2185.18 → TP2 @2304.10  **+$114.75**
- LONG  @2190.46 → TP2 @2352.12  **+$155.61**
- LONG  @2336.28 → 期末平仓       **+$20.42**

**亏损交易（16笔）：** 主要集中在1-3月ETH震荡上行期间做空被碾压，以及3月横盘期间的假突破。

---

## 🔧 ADX 计算公式（参考实现）

```python
def calculate_adx(highs, lows, closes, period=14):
    # True Range
    tr = pd.concat([
        highs - lows,
        (highs - closes.shift(1)).abs(),
        (lows - closes.shift(1)).abs()
    ], axis=1).max(axis=1)

    # Directional Movement
    up_move  = highs.diff()
    down_move = -lows.diff()
    plus_dm  = ((up_move > down_move) & (up_move > 0)) * up_move
    minus_dm = ((down_move > up_move) & (down_move > 0)) * down_move

    atr     = tr.ewm(alpha=1/period, adjust=False).mean()
    plus_di = 100 * (plus_dm.ewm(alpha=1/period, adjust=False).mean() / atr)
    minus_di= 100 * (minus_dm.ewm(alpha=1/period, adjust=False).mean() / atr)

    dx = 100 * (plus_di - minus_di).abs() / (plus_di + minus_di)
    adx = dx.ewm(alpha=1/period, adjust=False).mean()
    return adx
```

---

## 📄 License

MIT License

---

*策略版本：V2.0 | 适用平台：OKX Agent Trade Kit*
*进化自：少即是多 Less-is-More V1.0*
*核心改进：增加 ADX 趋势持续性确认层，过滤震荡市假信号，减少止损损耗*
